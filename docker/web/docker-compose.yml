version: "3.9"
services:
  nginx:
    image: nginx:latest
    container_name: nginx_reverse
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    networks:
      - infra_net

networks:
  infra_net:
    external: true